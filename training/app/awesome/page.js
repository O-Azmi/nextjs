// Routing works in next js by creating a new folder with page name you want. 
// Then create a file called page.js, it behaves exactly like react.

export default function awesomePage()
{
    return(
        <main>
            <h1>NextJs is Awesome</h1>
        </main>
    )
}